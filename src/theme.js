import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6F74C2',
    },
    secondary: {
      main: '#8B91DB',
    },
    background: {
      default: '#FFFFFF',
      paper: '#DADBED',
    },
    text: {
      primary: '#630083',
      secondary: '#6F74C2',
    },
    divider: '#DEB8EA',
  },
});

export default theme;
